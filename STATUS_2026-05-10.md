# 進捗状況 (2026-05-10 深夜時点)

## TL;DR

- VLE rigorous solver (Wang-Henke) 実装完了、Newton+Wegstein で 6.5× 高速化
- 重大バグ **3 件** 発見・修正:
  1. `B_bottoms` 取り違え (Dist1 成分マスバランス破綻)
  2. `V[N_feed]` off-by-one (Dist2 C2H6 recovery 崩壊)
  3. rigorous の utility 流用 (Dist2 silent FUG fallback、Profit 1.16 億円/年過小評価)
- BO 安全用 strict_recovery_check + solve_flowsheet 例外 catch 追加
- 20 ケース false convergence sweep でシステム堅牢性確認 (false convergence ゼロ)
- **`src/eos.py:bubble_point_T` を thermo (CalebBell, MIT v0.6.0) で内部置換**:
  cubic root 切替境界での偽根問題が根本解決、post-validate 対症療法を撤去 (= 綺麗)。
  外向き API 不変 (= wrapper パターン)。requirements.txt と pyproject.toml で固定。
- always-on validation を `RigorousResult` に組込で再発防止
- 48 項目 regression test (`tests/validate_wang_henke.py`) 全 PASS
- 当初「Dist2 stage 1 で 19K T outlier」は **`src/eos.py:bubble_point_T` の偽根**
  が原因、Wang-Henke 自体は 1.5K 精度で正しかった (validation 修正済)
- **`exp/exp2.py` 新規**: 塔単独 FUG vs rigorous 比較ツール (速い)
- exp1 全塔 rigorous で **~120 秒**、Profit (Stage 2) **+51.75 億円/年**
- 試した recovery post-check は exp1 recycle iter で誤発動するため撤去 (詳細は本文参照)

## 本日の主要成果

### 1. VLE rigorous solver 実装完了 (Wang-Henke 法)
- `src/distillation_rigorous.py` 新規作成 (~430 行)
- 切替フラグ: `ColumnTunables.solver_method = 'fug' | 'rigorous'`
- デフォルトは `'fug'` (高速、BO 用)
- `'rigorous'` は VLE tray-by-tray (top-k 検証用)
- 出典: Seader/Henley/Roper "Separation Process Principles" 3rd ed., Ch.10.4

### 2. 高速化: Newton 1ステップ + Wegstein 加速
- bubble-point の brentq → Newton 1 step (Clausius-Clapeyron 微分)
- 固定 damping → Wegstein 適応 damping (q ∈ [-5, 0])
- **約 6.5× 高速化** (701s → 107s 全塔 rigorous)
- FUG: 約 3 秒 (基準)

### 3. 連続発見・修正した重大 bug

**Bug 1: B_bottoms 取り違え** (`src/distillation_rigorous.py:550 付近`)
- リボイラ方程式で `L[N_stages]` (= `L_bot = R·D + q·F`) を bottoms 産物流量 `B = F − D` と取り違えていた
- Dist1 で 20× 過大 → C3H8 ↔ C4H10 が 102 kmol/h 入れ替わる
- **修正**: `B_bottoms` を関数引数で明示的に渡す形に変更

**Bug 2: V[N_feed] off-by-one** (V/L プロファイル設定)
- フィード段の上向き気流量 `V[N_feed]` を `V_bot` としていた
- 本来 `V_top = V_bot + (1-q)F`、フィード上に出る方
- Dist2 で C2H6 recovery が 12.8% に崩壊 → 修正後 99% へ
- **修正**: `if j <= N_feed: V[j] = V_top else V_bot` に変更

### 4. always-on validation 組込
`RigorousResult` に下記フィールド追加 (収束時に毎回計算):
- `mesh_residual_max`, `mesh_residual_mean`
- `component_balance_max`

`_simulate_rigorous` で:
- MESH 残差 > 0.01 → 例外 → FUG fallback
- 成分バランス > 1% → 例外 → FUG fallback

これで **同種の bug の再発を早期検知** できる仕組みに。

### 5. Regression test suite 化
`tests/validate_wang_henke.py` を恒久テストに昇格 (旧版は使い捨て)。
全 48 項目検査、Dist1/Dist2/Dist3 全塔で **PASS**:
- A. 収束、B. trivial K=1 検出、C. T プロファイル vs brentq、D. MESH 残差
- E. 総マスバランス、F. 成分マスバランス、G. recovery spec 達成
- H. FUG vs rig 出力一致、I. 熱量妥当性、J. 内蔵 validation

実行: `.\.venv\Scripts\python.exe tests\validate_wang_henke.py`

---

---

## BO 安全のための多層 safety net

| Layer | 検出する失敗 | 実装場所 |
|---|---|---|
| ① FUG infeasibility | N < N_min, R < R_min | `_simulate_fug` |
| ② Solver-level penalty | PSA/Mem CAPEX 暴走、recycle 発散 | solver の inner_status |
| ③ MESH/成分バランス | Wang-Henke 内部不整合 (B_bottoms 級バグ) | `_simulate_rigorous` の always-on validation |
| ④ strict_recovery_check (opt-in) | rigorous の "数値収束したが non-spec" 解 | `evaluate(strict_recovery_check=True)` |
| ⑤ Product spec check (soft) | 下流伝播後の C3H6/H2 純度・生産量違反 | `evaluate` 末尾 |
| ⑥ 例外 catch | 未処理 ValueError 等 (e.g., 流量ゼロ伝播) | `evaluate` 入口 |

**20 ケース sweep (2026-05-10 実施)**: 物理的に変な設計を多数試行 → false convergence (= spec も通り Profit も良いが physical に変) **ゼロ件**。

---

## 既知の限界 (修正困難、文書化のみ)

### ~~Dist2 partial_condenser stage 1 の T outlier~~ — 解決
**追加調査の結果、これは偽問題でした**:

実際のデータ (T scan、2026-05-10 終夜実施):
- T = 180K 以下: PR EOS が単相領域 (Z_V = Z_L = 0.029)、K=1 trivial、f ≈ 0
- T = 181K: vapor root が突然出現、K's 急変、f が -0.27 に **不連続にジャンプ**
- T = 198.7K (-74.45°C): 真の bubble point (f が連続的にゼロ交差)
- Wang-Henke 解 200K (-73.15°C): **真の root から 1.5K しかズレてない**

→ src/eos.py の `bubble_point_T` (brentq) が PR EOS の不連続点を root と
  誤認識して T = 180.7K (f = -0.30) を返していた。**Wang-Henke は正解**、
  brentq が偽根を返していた。

validation script 修正済み (`f(brentq_T)` を計算して |f| > 0.01 なら偽根扱い)、
全 48 PASS 維持。

### 残る課題 (修正候補、優先度低)
- `src/eos.py:bubble_point_T` を改善して spurious root を返さないようにする
  (現状は validation でフィルタしてるだけ、production code では偽根が返ることがある)
- 影響度: 低 (production の rigorous solver は brentq に依存していない、
  validation の検査基準だけが影響受ける)

### 速度
- 全塔 rigorous: 107s
- BO ループには重すぎ → 引き続き **BO は FUG**、検証時のみ rigorous 推奨

---

## ファイル変更履歴 (本日)

| ファイル | 変更内容 |
|---|---|
| `src/distillation_rigorous.py` | 新規作成 (Wang-Henke + Newton + Wegstein + always-on validation) |
| `src/distillation_core.py` | dispatch + `_simulate_rigorous` + utility 再選択、partial_cond C3 補正 |
| `src/eos.py` | PR EOS Ω_a/Ω_b citation 追記 + **bubble_point_T を thermo で内部置換** |
| `flowsheet/runner.py` | `evaluate(strict_recovery_check=...)` + 未処理例外 catch |
| `units/separators/column1/2/3.py` | `solver_method` プロパゲート |
| `exp/exp1.py` | `SOLVER_DIST1/2/3` 個別フラグ追加 |
| `exp/exp2.py` | 新規 (塔単独 FUG vs rigorous 比較ツール) |
| `tests/validate_wang_henke.py` | 新規 (48 項目 regression suite) |
| `requirements.txt` | 新規 (thermo==0.6.0 ほか pin) |
| `pyproject.toml` | 新規 (Python 3.13 ほか pin、PEP 621 形式) |
| `.python-version` | 新規 (pyenv 用、Python 3.13.0) |
| `KNOWN_PLACEHOLDERS.md` | bug 修正・citation 履歴更新 |

---

## 明日の再開メモ

### exp1.py 現状設定
```python
SOLVER_DIST1 = 'rigorous'    # narrow-margin、VLE で物理確認
SOLVER_DIST2 = 'rigorous'    # partial cond
SOLVER_DIST3 = 'rigorous'    # narrow-α、~10 分かかる
```

### 動作確認コマンド
```powershell
# Regression test (高速、~30 秒)
.\.venv\Scripts\python.exe tests\validate_wang_henke.py

# 完全 exp1 (約 2 分、全塔 rigorous)
.\.venv\Scripts\python.exe exp/exp1.py
```

### 残課題候補

1. **PR EOS の numpy ベクトル化** (各 K 計算を全段一括)
   - 工数: 半日
   - 効果: 2-3× 高速化見込み
   - 必要性: 現状 107 秒で実用範囲、優先度低

2. **BO への組込** (FUG ベース、rigorous は top-k 検証時のみ呼ぶ)
   - 工数: 1 日
   - 既存 BO フレームワーク次第
   - `evaluate(strict_recovery_check=True)` で BO 安全保証

3. **(オプション) thermo パッケージで src/eos.py 全面置換**
   - bubble_point_T 偽根問題が根本解決
   - 工数: 半日 + 全 production code の適合作業
   - 必要性: 現状の偽根検出 (post-validate NaN) で実用上問題なし

### ベースライン値 (全塔 rigorous + thermo bubble_point_T、2026-05-10 最終)

| 指標 | 値 |
|---|---|
| 実行時間 | ~127 秒 (thermo overhead +8%) |
| Reactor 転化率 | 32.3% |
| Reactor 選択率 | 75.1% |
| C3H6 純度 (Dist3 塔頂) | **99.982 wt%** ✓ |
| H2 純度 (PSA 製品) | **99.997 mol%** ✓ |
| 生産量 | **1181.07 kmol/h** ✓ |
| Profit (HI 後) | **+53.59** 億円/年 |
| **Profit (Stage 2)** | **+51.78** 億円/年 |

**警告・エラー**: なし。Validation 48/48 PASS、20 ケース false convergence sweep で robust 確認。
thermo 採用前 (+51.75) との差は +0.03 億円 = numerical noise 同等、実質変化なし。

### 私 (Claude) が実施した内容まとめ
1. ✓ Wang-Henke の B_bottoms バグ + V[N_feed] off-by-one バグを修正
2. ✓ Newton 1ステップ + Wegstein 加速 で 6.5× 高速化
3. ✓ partial_condenser stage 1 の T 凍結を試みたが MESH 残差悪化で revert
4. ✓ always-on validation を RigorousResult に組込
5. ✓ Regression test (`tests/validate_wang_henke.py`、48 検査) 整備、全 PASS
6. ✓ 最終 baseline 取得・記録 (上表)
7. ✓ trivial K=1 検出を `_bubble_T_newton_step` に追加→ overhead 9s に対し利益なしで削除
8. ✓ **Dist2 stage 1 "19K outlier" の真因解明**: brentq が偽根を返していた、Wang-Henke は実は正解 (1.5K 精度)
9. ✓ validation script の brentq 偽根検出を強化 (f(T*) ≈ 0 を要求)
10. ✓ **`exp/exp2.py` 新規作成**: 蒸留塔単独 (FUG vs rigorous) 比較ツール、リサイクル不要で高速デバッグ可能
11. ✓ **utility 再選択 bug 修正** (`_simulate_rigorous`): rigorous の T_top で utility を再選択するように、これまで FUG の utility を流用していて Dist2 で ΔT 違反 → silent FUG fallback していた
    - Profit (Stage 2) +50.59 → **+51.75 億円/年 (+1.16)** 改善
12. ✓ FUG の bubble_point_T 偽根検査 — 全 3 塔で問題なし (健全)
13. ✓ `src/eos.py:bubble_point_T` に偽根検出 (post-validate f(T*) ≈ 0) を追加
14. ✓ **速度 vs 精度トレードオフ検証**: tol=0.05K (default) vs 1e-5K (極厳) で結果比較
    - T_top/T_bot/主要 F は tolerance に依存せず essentially fixed
    - iter は tol を 5000× 厳しくしても 2× しか増えない → Newton+Wegstein は本質的に効率的
    - 「VLE 早すぎ」疑念は **algorithmic improvement、早期打ち切りではない** と確認
15. ✗ **recovery spec post-check を `_simulate_rigorous` 内に試みて撤去**:
    - 当初: rigorous の rating mode で N=N_min ぎりぎりだと non-spec 解を返すことを懸念
    - 実装: ±5% threshold で fallback、その後 ±20% に緩和したが改善せず
    - 失敗の根本原因: exp1 recycle iter 中の transient state で feed 組成が大きく変動
      → 一時的に recovery spec から大幅外れて誤発動 → silent fallback で Profit 1 億円/年 過小評価
    - `flowsheet/run_one_pass.py:94` の `warnings.simplefilter("ignore")` で fallback warning も
      抑制されてて発見が遅れた (debugging に時間かかった)
    - 撤去後: Profit (Stage 2) +50.69 → +51.75 (真の rigorous 結果) 復活
    - 学び: rating mode の本質 (recovery が出力) を尊重、catastrophic ケース (FUG infeasible
      になる N=1 等) は既存 safety net で十分
16. ✓ **`evaluate(strict_recovery_check=True, recovery_tolerance=0.10)` 実装**: BO 用の post-check
    - exp1 outer-loop 収束後 (= 全体安定状態) に各塔 recovery を spec ±10% で検査
    - recycle iter の transient で誤発動しない (= column 内じゃなく runner.py レベルでチェック)
    - 12 ケース判定テスト: D3 (Dist3 N=82 ぎりぎり) と E (Dist1 P=80bar) を catch ✓、
      baseline・margin OK 設計は通過 ✓
17. ✓ **`solve_flowsheet` 例外 catch 追加**: ValueError 等の uncaught 例外を penalty 化
    - 旧版: BO がクラッシュ (例: Dist1 N=12 で expansion_valve 流量ゼロ)
    - 新版: `effective_TAC=10000` で penalty 返却、BO は安全に進行
18. ✓ **20 ケース false convergence sweep 実施**: 物理的に変な設計を多数試行
    - 結果: false convergence ゼロ (全 20 ケースで正しく判定)
    - 内訳: 17 infeasible (各 layer で catch)、3 feasible (実は正当な over/under-design)
    - **多層 safety net (4 layer) が機能してる確認**:
      ① FUG infeasibility (N<N_min, R<R_min)
      ② Solver-level penalty (PSA/Mem CAPEX 暴走)
      ③ strict_recovery_check (rigorous の non-spec 解)
      ④ Product spec check (下流 spec 違反 soft penalty)
19. ✓ **thermo パッケージで bubble_point_T を内部置換**:
    - 当初 src/eos.py の手作り PR EOS が cubic root 切替境界で偽根を返す問題発覚
    - 対症療法 (post-validate で NaN) で対処してたが原則的に悪手
    - thermo (CalebBell, MIT v0.6.0) で内部置換、外向き API 不変 (wrapper パターン)
    - 偽根問題が根本解決、post-validate 撤去、コード綺麗
    - 動作: Validation 48/48 PASS、exp1 結果 +0.03 億円/年 差 (numerical noise)
    - thermo overhead で実行時間 +8% (118s → 127s)、許容範囲
    - バージョン pin: requirements.txt + pyproject.toml + .python-version で 3 重防御
20. ✓ **依存環境固定** (Python 3.13.0、thermo==0.6.0 等):
    - requirements.txt (pip 用)
    - pyproject.toml (PEP 621 形式、modern tooling 用)
    - .python-version (pyenv 用)

### 私 (Claude) が触らなかった点
- 個別塔のソルバ選択 (SOLVER_DIST1/2/3) — ユーザー設定維持 (現在は全 'rigorous')
- Profit 改善のための設計変数いじり

### 明日の再開時に確認すべきコマンド

```powershell
# 1. Regression test (~30 秒、全 PASS のはず)
.\.venv\Scripts\python.exe tests\validate_wang_henke.py

# 2. exp1 (現在 全塔 rigorous で ~100 秒)
.\.venv\Scripts\python.exe exp/exp1.py
```

おやすみなさい。
