# Python / Jupyter
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
data/
# Claude Code等のAIツールの履歴があれば（任意）
.claude/

# 実験出力 (各実行毎に生成、版管理対象外)
outputs/

# エディタ/IDE 設定 (.venv 同様マシン固有。共有はドライブ経由で十分)
.vscode/

# tools/ の一時診断スクリプト (_ 接頭辞 = 使い捨て。本採用ツールは _ なしで追跡)
tools/_*.py
# 公開対象外の退避置き場(ローカル保持・追跡しない)
/bin/
